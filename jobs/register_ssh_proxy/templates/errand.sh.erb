#!/bin/bash

set -x
set -e

curl -X POST --data '[{"external_port": 2222, "backends" : [ {"ip": "ssh-proxy.service.cf.internal", "port":2222} ]}]' \
  http://router-configurer.service.cf.internal:9999/v0/external_ports
