#!/bin/bash

set -e

declare -a arr=("healthcheck.tgz" "lattice-cell-helpers.tgz")

for (( i = 0; i < ${#arr[@]} ; i++ )); do
  filename=/var/vcap/packages/ltc-assets/${arr[$i]}
  echo "Uploading $filename"
  curl -X POST -d @$filename http://<%= p("lattice.assets.file_server") %>/v1/static/
done
