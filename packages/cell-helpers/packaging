set -e -x

export GOROOT=$(readlink -nf /var/vcap/packages/golang1.4)
export PATH=$GOROOT/bin:$PATH
export GOPATH=$PWD/lattice/cell-helpers

go install tee2metron
cp -a $PWD/lattice/cell-helpers/bin/tee2metron ${BOSH_INSTALL_TARGET}
